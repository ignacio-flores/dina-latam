
*! classname: twowaygraph_g
*! family: twoway
*! date:  1 Nov 2019
*! time: 16:17:51
*! graph_scheme: s2color
*! naturallywhite: 1
*! end

// File created by Graph Editor Recorder.
// Edit only if you know what you are doing.

gr_edit .style.editstyle margin(bottom) editcopy
gr_edit .style.editstyle boxstyle(shadestyle(color(white))) editcopy
gr_edit .style.editstyle boxstyle(linestyle(color(white))) editcopy
// Graph color

gr_edit .xaxis1.reset_rule 2000 2017 4 , tickset(major) ruletype(range) 
// xaxis1 edits

gr_edit .xaxis1.title.text = {}
gr_edit .xaxis1.title.text.Arrpush BRA
// title edits

gr_edit .legend.plotregion1.label[1].text = {}
gr_edit .legend.plotregion1.label[1].text.Arrpush bot 50%
// label[1] edits

gr_edit .legend.plotregion1.label[2].text = {}
gr_edit .legend.plotregion1.label[2].text.Arrpush mid 40%
// label[2] edits

gr_edit .legend.plotregion1.label[3].text = {}
gr_edit .legend.plotregion1.label[3].text.Arrpush top 10%
// label[3] edits

gr_edit .legend.plotregion1.label[4].text = {}
gr_edit .legend.plotregion1.label[4].text.Arrpush top 1%
// label[4] edits

gr_edit .style.editstyle boxstyle(shadestyle(color(white))) editcopy
gr_edit .style.editstyle boxstyle(linestyle(color(white))) editcopy

gr_edit .legend.plotregion1.label[5].text = {}
gr_edit .legend.plotregion1.label[5].text.Arrpush Gini

gr_edit .yaxis1.reset_rule 0 0.7 0.1 , tickset(major) ruletype(range) 
// label[5] edits


// <end>
